services:
  frontend:
    build:
      context: ./client
    environment:
      - CHOKIDAR_USEPOLLING=true
    volumes:
      - ./client:/app
      - node_modules:/app/node_modules
    ports:
      - '3000:3000'

volumes:
  node_modules:
